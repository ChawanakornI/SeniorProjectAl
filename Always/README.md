# always
- `lib/main.dart` wires up the app’s entry point, themes, routes, and launches the correct home screen.  GP and the Expert doctors
- `lib/routes.dart` centralizes named routes for login, home (doctor), and GP home variants.  
- `lib/features/login/presentation/widgets/login_form.dart` implements the login UI, credential CSV parsing with name/role, “remember me” persistence, and role-based navigation to the appropriate home page.
- `lib/pages` for pages: notification, home page, profile page, setting page
- `lib/theme` them glassmorphism
